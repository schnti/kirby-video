{
  "name": "schnti.video",
  "description": "",
  "private": true,
  "scripts": {
    "build": "sass --no-source-map --style=expanded src/youtube.scss dist/youtube.css && sass --no-source-map --style=compressed src/youtube.scss dist/youtube.min.css",
    "watch": "sass --watch --no-source-map --style=expanded src/youtube.scss dist/youtube.css",
    "start": "npm run build"
  },
  "devDependencies": {
    "sass": "^1.77.8"
  },
  "dependencies": {}
}
